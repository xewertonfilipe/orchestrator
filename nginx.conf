server {
  listen 80;
  server_name localhost;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    # SPA fallback: direct access to /home and other client routes must return index.html.
    try_files $uri $uri/ /index.html;
  }
}
